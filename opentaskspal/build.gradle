apply plugin: 'com.android.library'

android {
    compileSdkVersion COMPILE_SDK_VERSION.toInteger()
    buildToolsVersion BUILD_TOOLS_VERSION

    defaultConfig {
        minSdkVersion MIN_SDK_VERSION.toInteger()
        targetSdkVersion TARGET_SDK_VERSION.toInteger()
    }
}

dependencies {
    compile project(':opentasks-contract')
    compile 'org.hamcrest:hamcrest-library:1.3'
    // TODO Update to contentpal master version once all pending PRs have been merged there:
    compile 'com.github.dmfs.contentpal:contentpal:07319b058155c0d6703f3c3725540bb85ca9700a'
}